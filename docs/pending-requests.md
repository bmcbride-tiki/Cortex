# Pending Requests — as of 2026-07-30

Captured at the point `main` was set as the new baseline (commit `9b18a22`, after the Node Configuration panel reorder and the Conditions workflow node landed). Everything below existed only as uncommitted working-tree changes or an informal todo note before this point — none of it is in `main`'s history. Review each item against the current codebase before re-requesting; line numbers, file contents, and some conventions (e.g. the database filename) have moved since these were written.

## From zzzz_todo.txt

1. ~~**ABC Builder: change taxonomy to Public**~~ — **Done** (commit `55a2a9f`). ABC Builder is an external harness that only talks to public-facing agent-builder models; it's now tagged with a dedicated `"agenty"` model key mapped to the `"public"` classification level, so adding it to a workflow caps the Workflow Classification badge at Public via the existing "most restrictive model wins" ceiling logic — verified live (badge reads "Workflow Classification: Public" once an ABC Builder node is on canvas).
2. **500 server error — add error log to server.py / API load** — no further detail was recorded. Needs scoping: which endpoint(s) produced the 500, and whether this means general server-side error logging (e.g. a catch-all exception logger) or something specific to app/API loading at startup.

## Discarded uncommitted changes (documented here, not carried forward)

These were complete, working diffs sitting uncommitted in the working tree — reverted at the user's request rather than committed, so they can be re-requested deliberately against the current baseline instead of landing silently.

3. **Database file rename: `brain_state.db` → `cortex.db`.** Touched `00_System/database.py`'s header comments and `12_Tasks/abc_uploader/abc_uploader.py`/`.md`'s references, updating them to describe the DB file as `00_System/cortex.db` instead of `brain_state.db`. **Note:** these were doc-comment-only changes — if the actual database filename should be `cortex.db` going forward, the real rename (wherever the filename constant/connection string is actually defined) still needs to happen; this diff only updated comments/docs referencing it.
4. **`server.py` dev port: 8080 → 8090.** The `uvicorn.run(..., port=...)` call in `server.py`'s `__main__` block was changed from 8080 to 8090.
5. **`CLAUDE.md`: add a "99_Outbox ground rule" section.** Documented that every file generated by any tool (`10_Skills`–`14_Adapters`) must be written to `99_Outbox/<tool_name>/` at the vault root, never into the tool's own source folder.
6. **`word_to_excel_exam`: adopt the 99_Outbox ground rule + add a file-picker dialog.**
   - Output moved from a local `12_Tasks/word_to_excel_exam/output/` folder to `99_Outbox/word_to_excel_exam/`.
   - When run directly with no CLI argument, it now opens a native file-explorer dialog (via `tkinter.filedialog`) to select the source `.docx` instead of requiring a typed/pasted path.
   - `.md` doc updated to match both changes, plus a new "Ground Rule: Output Location" section referencing the `curriculum_guide_to_tos` convention.

Items 5 and 6 are related: 6 is what applying 5's rule to this specific tool looks like. If the 99_Outbox ground rule is still wanted project-wide, re-requesting 5 first and then re-deriving 6 (or any other tool that still writes to its own folder) is the cleaner order — check what else in `12_Tasks/`/`13_Functions/` still writes locally.

## Other outstanding planning artifact (not discarded, left in place)

7. **NotebookLM browser automation** — a full implementation plan already exists, untracked, at `docs/superpowers/plans/2026-07-29-notebooklm-browser-automation.md`. Not part of this cleanup; still there to pick up whenever it's prioritized.
